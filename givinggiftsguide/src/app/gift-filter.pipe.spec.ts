import { GiftFilterPipe } from './gift-filter.pipe';

describe('GiftFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new GiftFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
